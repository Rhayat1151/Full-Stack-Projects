<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Add a New Book</title>
  <link rel="stylesheet" href="/styles/style.css">
  <style>
    /* General styles */
    body {
      font-family: 'Georgia', serif;
      background: linear-gradient(135deg, #f0f0f0, #d0d0d0);
      background-size: 200% 200%;
      animation: backgroundAnimation 10s ease infinite;
      color: #333;
      margin: 0;
      padding: 40px;
      line-height: 1.6;
    }

    @keyframes backgroundAnimation {
      0% { background-position: 0% 0%; }
      50% { background-position: 100% 100%; }
      100% { background-position: 0% 0%; }
    }

    header {
      text-align: center;
      margin-bottom: 30px;
    }

    h1 {
      font-size: 2.5em;
      font-weight: bold;
      margin: 0;
    }

    .show-form-button {
      background-color: #333;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 1em;
      cursor: pointer;
      margin-top: 20px;
      display: block;
      margin: 20px auto;
      border-radius: 5px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: background-color 0.3s, box-shadow 0.3s;
      text-decoration: none;
      text-align: center;
    }

    .show-form-button:hover {
      background-color: #555;
      box-shadow: 0 6px 8px rgba(0, 0, 0, 0.2);
    }

    .new-book-form {
      background-color: white;
      padding: 20px;
      box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
      width: 50%;
      margin: 0 auto;
      border-radius: 10px;
    }

    .new-book-form form {
      display: flex;
      flex-direction: column;
    }

    .new-book-form label {
      margin-bottom: 5px;
      font-weight: bold;
    }

    .new-book-form input,
    .new-book-form textarea {
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ddd;
      border-radius: 5px;
    }

    .new-book-form button {
      background-color: #333;
      color: white;
      border: none;
      padding: 10px 20px;
      font-size: 1em;
      cursor: pointer;
      border-radius: 5px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: background-color 0.3s, box-shadow 0.3s;
    }

    .new-book-form button:hover {
      background-color: #555;
      box-shadow: 0 6px 8px rgba(0, 0, 0, 0.2);
    }
  </style>
</head>
<body>
  <header>
    <h1>Add a New Book</h1>
    <a href="/" class="show-form-button">Back to Books List</a>
  </header>

  <div class="new-book-form">
    <form action="/books/add" method="POST">
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" required><br>

      <label for="rating">Rating (out of 5):</label>
      <input type="number" id="rating" name="rating" step="0.1" min="0" max="5" required><br>

      <label for="recency">Date Read:</label>
      <input type="date" id="recency" name="recency" required><br>

      <label for="notes">Notes:</label>
      <textarea id="notes" name="notes" rows="4" required></textarea><br>

      <button type="submit">Add Book</button>
    </form>
  </div>
</body>
</html>

